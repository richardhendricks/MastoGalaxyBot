import sqlite3

print("This script extracts the image data from the image provider and incorporates it into a database")
print("<database location> Where to write the database")
print("<image provider> Currently supports only Smugmug")
print("<album base URL> Base album to use for image data")
